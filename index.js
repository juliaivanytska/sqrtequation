var a = prompt('Введіть значення для a',100000);
var b=prompt ('Введіть значення для b',100000);
var c=promt ('Введіть значення для c',100000);
var D=number;
//sqrt 
function discriminant () {document.write('D'); D='b*b+4*a*c';}
discriminant();
function kvadrat(square root) {
}
if {
    
} (D=0) {
   x=(-b)/2*a
}
if{
if(D<0) {
    x="НЕМА"
}
else{
if (D>0) {x=((-b)+sqrt(D))/(2*a)} {x=((-b)+sqrt(D))/(2*a)};}
   document.write('квадратний корінь дискримінанта' +Math.sqrt(D)+'<br>');
   document.write(<br/>)
  document.write('x' +x+ '<br>');
  document.write(<br/>)
